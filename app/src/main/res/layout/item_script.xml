<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Widget.AutoMacro.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <View
            android:layout_width="5dp"
            android:layout_height="match_parent"
            android:background="@color/brand_violet" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:orientation="vertical"
            android:paddingHorizontal="14dp"
            android:paddingVertical="12dp">

            <TextView
                android:id="@+id/tvScriptName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-medium"
                android:textSize="16sp"
                tools:text="Kịch bản gửi tin" />

            <TextView
                android:id="@+id/tvScriptStepCount"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:alpha="0.7"
                android:textSize="13sp"
                tools:text="7 bước" />

        </LinearLayout>

        <ImageButton
            android:id="@+id/btnRunScript"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_gravity="center_vertical"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_icon_button_tonal"
            android:contentDescription="Chạy kịch bản"
            android:padding="9dp"
            android:src="@android:drawable/ic_media_play"
            app:tint="@color/brand_violet" />

        <ImageButton
            android:id="@+id/btnDeleteScript"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_gravity="center_vertical"
            android:layout_marginEnd="12dp"
            android:background="@drawable/bg_icon_button_tonal"
            android:contentDescription="Xóa kịch bản"
            android:padding="10dp"
            android:src="@android:drawable/ic_menu_delete"
            app:tint="@color/hub_bad" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
